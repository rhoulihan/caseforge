{
  "_comment": "Fictional MongoDB Atlas Admin API v2 'describe cluster' output for a sample Atlas customer (Meridian Freight Systems). Illustrative only — not a real cluster. Shape mirrors GET /api/atlas/v2/groups/{groupId}/clusters/{name}.",
  "name": "meridian-prod",
  "clusterType": "SHARDED",
  "mongoDBMajorVersion": "7.0",
  "mongoDBVersion": "7.0.12",
  "diskSizeGB": 2500,
  "backupEnabled": true,
  "pitEnabled": true,
  "replicationSpecs": [
    {
      "zoneName": "Zone 1",
      "numShards": 3,
      "regionConfigs": [
        {
          "providerName": "AWS",
          "regionName": "US_EAST_1",
          "priority": 7,
          "electableSpecs": { "instanceSize": "M50", "nodeCount": 3, "diskIOPS": 3000, "ebsVolumeType": "PROVISIONED" },
          "analyticsSpecs": { "instanceSize": "M50", "nodeCount": 0 },
          "readOnlySpecs": { "instanceSize": "M50", "nodeCount": 0 }
        },
        {
          "providerName": "AWS",
          "regionName": "US_WEST_2",
          "priority": 6,
          "electableSpecs": { "instanceSize": "M50", "nodeCount": 2, "diskIOPS": 3000, "ebsVolumeType": "PROVISIONED" },
          "analyticsSpecs": { "instanceSize": "M50", "nodeCount": 0 },
          "readOnlySpecs": { "instanceSize": "M50", "nodeCount": 0 }
        }
      ]
    }
  ],
  "biConnector": { "enabled": false },
  "encryptionAtRestProvider": "AWS",
  "labels": [{ "key": "env", "value": "production" }],
  "stateName": "IDLE",
  "notes": "3 shards. Home region us-east-1 (3 electable M50 per shard). Cross-region warm DR in us-west-2 (2 electable M50 per shard). M50 = 8 vCPU / 32 GB RAM per node. Logical data ~1.8 TB; provisioned disk 2500 GB/node."
}
